# Class definition for the Player character
class Player:
    # Initialize a new player instance
    def __init__(self, x: int, y: int):
        # Store the player's x coordinate (horizontal position on the grid)
        self.x = x
        # Store the player's y coordinate (vertical position on the grid)
        self.y = y
        
        
